spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ordify
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: falses
    baseline-on-migrate: true
    locations: classpath:db/migration